"use client"

import { useSearchParams } from "next/navigation"
import { useRef, useEffect} from "react"

export default function CreatEventDialogue({title, onClose, children}){

    return (
        <div>
            Dialog
        </div>
    )
}